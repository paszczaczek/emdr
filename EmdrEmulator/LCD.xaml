<UserControl x:Class="EmdrEmulator.LCD"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EmdrEmulator"
             mc:Ignorable="d"
             d:DataContext="{x:Static local:LCD+Model.DesignTime}" >
    <StackPanel Background="#FF4C8DFF" Width="180">
        <StackPanel.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontFamily" Value="Arcade"/>
                <Setter Property="FontSize" Value="21"/>
                <Setter Property="Margin" Value="5,0"></Setter>
            </Style>
        </StackPanel.Resources>
        <TextBlock Text="{Binding Line1}"/>
        <TextBlock Text="{Binding Line2}"/>
    </StackPanel>
</UserControl>
