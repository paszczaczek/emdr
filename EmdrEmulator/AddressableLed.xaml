<UserControl x:Class="EmdrEmulator.AddressableLed"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EmdrEmulator"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
            d:DataContext="{x:Static local:AddressableLed+MyDataContext.DesignTime}"
             mc:Ignorable="d">
    <Grid>
        <Grid.Resources>
            <system:Double x:Key="radius">80</system:Double>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Ellipse x:Name="el" Grid.Row="0" Stroke="Gray" Fill="{Binding Brush}" Width="{Binding Mode=OneWay, Source={StaticResource radius}}" Height="{Binding Mode=OneWay, Source={StaticResource radius}}"/>
        <TextBlock Grid.Row="1" Text="{Binding Path=LedNumber}" HorizontalAlignment="Center"/>
    </Grid>
</UserControl>
