<Window x:Class="EmdrEmulator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EmdrEmulator"
        mc:Ignorable="d"
        d:DataContext="{x:Static local:MyDataContext.DesignTime}"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ScrollViewer x:Name="serialMonitorScrollViewer" Grid.Row="1">
            <TextBlock x:Name="serialMonitor" Text="{Binding SerialMonitor}"/>
        </ScrollViewer>
    </Grid>
</Window>
