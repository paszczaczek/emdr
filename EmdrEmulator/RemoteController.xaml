<UserControl x:Class="EmdrEmulator.RemoteController"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EmdrEmulator"
             mc:Ignorable="d"
             Background="Black">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.Resources>
            <Style TargetType="Button">
                <Setter Property="Background" Value="White"/>
                <Setter Property="Foreground" Value="Black"/>
            </Style>
        </Grid.Resources>
        
        <Button Grid.Row="0" Grid.Column="0" Content="UN" Tag="0"  Click="Button_Click"/>
        <Button Grid.Row="0" Grid.Column="1" Content="PR" Tag="1"  Click="Button_Click" />
        <Button Grid.Row="0" Grid.Column="2" Content="RR" Tag="2"  Click="Button_Click" />

        <Button Grid.Row="1" Grid.Column="0" Content="CH+" Tag="3" Click="Button_Click"/>
        <Button Grid.Row="1" Grid.Column="1" Content="CH-" Tag="4" Click="Button_Click"/>
    </Grid>
</UserControl>
