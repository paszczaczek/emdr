<UserControl x:Class="EmdrEmulator.RemoteController"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EmdrEmulator"
             mc:Ignorable="d"
             Background="Black">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.Resources>
            <Style TargetType="Button">
                <Setter Property="Background" Value="White"/>
                <Setter Property="Foreground" Value="Black"/>
                <Setter Property="Width" Value="20"/>
            </Style>
        </Grid.Resources>

        <Button Grid.Row="0" Grid.Column="0" Content="&#x23F5;" Tag="Start" Click="Button_Click"/>
        <Button Grid.Row="0" Grid.Column="1" Content="&#x25A0;" Tag="Stop" Click="Button_Click"/>
        <Button Grid.Row="0" Grid.Column="2" Content="&#x23F8;" Tag="Pause" Click="Button_Click"/>
        
        <Button Grid.Row="1" Grid.Column="0" Content="?"        Tag="?"  Click="Button_Click"/>
    </Grid>
</UserControl>
